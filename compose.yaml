services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ngo_receipts
      POSTGRES_USER: ngo
      POSTGRES_PASSWORD: ngo_local_only
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
